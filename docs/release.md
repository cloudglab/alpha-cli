# Release Notes

## 发布经验

- 命令系统有新增、删除或改名时，必须同时检查并同步 `skills/` 下的说明文档与参考文件。
- 如果项目存在源目录与发布目录双份 skills，两边都要改，不能只改一边。
- 发版前至少做三件事：
  - 重新生成或校验命令清单 / manifest / help 输出。
  - 检查 skills reference 中是否出现缺失命令、旧命令名或错误示例。
  - 确认构建产物里的实际 CLI 命令与源码注册一致。
